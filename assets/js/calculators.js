
(function(){'use strict';const staffCost=document.getElementById('staffCost'),employeeCount=document.getElementById('employeeCount'),planCost=document.getElementById('planCost'),monthlySavings=document.getElementById('monthlySavings'),annualSavings=document.getElementById('annualSavings'),savingsPercent=document.getElementById('savingsPercent');function formatAED(n){return 'AED '+Math.round(n).toLocaleString('en-AE')}function recalc(){if(!staffCost||!employeeCount||!planCost)return;const current=Number(staffCost.value||0)*Number(employeeCount.value||1);const ai=Number(planCost.value||0);const monthly=Math.max(current-ai,0);const annual=monthly*12;const pct=current?Math.round(monthly/current*100):0;if(monthlySavings)monthlySavings.textContent=formatAED(monthly);if(annualSavings)annualSavings.textContent=formatAED(annual);if(savingsPercent)savingsPercent.textContent=pct+'%'}[staffCost,employeeCount,planCost].forEach(el=>el&&el.addEventListener('input',recalc));recalc()})();
